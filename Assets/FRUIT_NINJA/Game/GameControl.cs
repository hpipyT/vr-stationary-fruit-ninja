using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameControl : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Time.timeScale = 0.5f;
    }
    // Update is called once per frame
    void Update()
    {
        
    }

    // cake spawns in places,
    // fruit spawns at x3 rate of cake

    // cake restores player health
    // player health drains slowly over time

    // fruits touching player stop movement 


}
